[pytest]
markers =
    smoke: fast checks of commands, configuration, and core audit behavior
    contract_security: configuration, evidence, path, redaction, and rendering contracts
    e2e: resumable workflow or cross-component end-to-end tests
